# 项目约束

- 在本仓库中完成请求的修改后，默认提交到当前分支并推送至已配置的远程仓库。
- 除非明确要求，否则不得强制推送或重写已发布历史。推送因认证、权限、网络或远程分支分叉等原因失败时，报告确切错误并保留本地提交。

# 流程权威性与交付目标

- 本项目用于将推理模型规范迁移并适配至昇腾 NPU，验收通过的实现拟合入 [ModelZoo-PyTorch `ACL_PyTorch/built-in`](https://gitcode.com/Ascend/ModelZoo-PyTorch/tree/master/ACL_PyTorch/built-in)。
- 本文件是常驻硬约束摘要；模型适配、验收和上库复核的完整要求以 `模型NPU 适配标准流程.md` 为权威来源。开始相关工作前必须读取并执行该流程，不得因本文件未重复某项细则而忽略它。
- [Ascend-SACT](https://ai.gitcode.com/Ascend-SACT) 的初步适配和目标仓近期模型均只作为迁移起点与审查参考。必须记录参考来源和版本，并按目标仓当前结构、规范、依赖、接口和验收要求复核，禁止未经分析直接复制。
- 每次开始模型适配或上库复核前，必须重新查询目标仓 `master` HEAD，不得长期复用流程文档中的历史快照。参考模型按最后实质变更时间排序：同领域且同推理形态、同推理后端、同领域历史模型；批量链接或格式修复不算实质变更。
- `NPU_ADAPTATION.md` 必须记录检查日期、目标仓 commit、参考目录、参考目录最后实质变更 commit/date、选择原因和拟合入路径 `ACL_PyTorch/built-in/<领域>/<模型目录>`。最新参考仍须核对 README 与实际文件、贡献规范、许可证、环境、数据/权重来源和验收口径；与当前硬门禁冲突时，以硬门禁和可执行事实为准并记录差异。
- 目标路径已存在时，必须先比较现有文件和 README，判定本次是新增、替换还是增量更新，并明确保留、替换和删除范围。

# 工作证据与正式上库产物

- 当前模型目录是迁移工作区，不等同于正式候选目录。`NPU_ADAPTATION.md`、`ACCEPTANCE_PLAN.md`、`README_old.md`、历史日志、下载权重、评测数据、虚拟环境、缓存、`upstream/` 和 `.codex-reference/` 默认不进入候选目录。固定的小型功能样例可进入，但 README 必须说明其仅用于链路验证。
- 默认候选包括 `README.md`、正式运行/评测/数据准备脚本、必要 patch/diff、依赖和许可证文件，以及当前贡献门禁要求的元数据。`NPU_ADAPTATION.md` 必须列出精确的“上库文件清单”和排除项。
- 上库 `README.md` 必须自包含，不得依赖默认排除的内部证据才能完成安装、推理、评测和结果理解。
- 候选目录不得依赖修改用户 site-packages、运行时 monkey patch、手工覆盖或未记录的本地状态。第三方源码变更必须以固定版本可重复应用的 patch/diff 或目标仓认可的受控源码文件交付。
- 技术验收与上库状态必须分开：达到 S3 仅表示 NPU L2 精度/质量与性能验收完成；目标路径、候选文件、README、许可证、贡献门禁和 clean-room 重放全部通过后，才可称“上库候选就绪”；只有目标仓已合并 PR 或 commit 才能证明“正式合入”。

# 验收与完成状态

- 每个模型必须先盘点原始测试集和原始指标，包括 dataset、split、样本规模、metric、normalizer/后处理、decode/推理参数、checkpoint/版本和来源链接。官方未发布时必须明确记录，不得编造或用自定义指标冒充。
- NPU 验收默认与原始公开/官方结果对齐。基线可来自论文、GitHub/HuggingFace 官方数据或业界公开数据，不强制在本环境重跑原始 CPU/CUDA；但 checkpoint、数据、metric、后处理或推理参数不可比时，必须补跑可比的本地同设备回归，或标记未完成，不得直接得出对齐结论。
- 交付状态统一使用 S0 分析完成、S1 静态适配完成、S2 功能验证完成、S3 L2 迁移对齐完成、S4 扩展验收通过。仅有版本取证、文档、patch apply/compile 或 smoke test 时，不得宣称“适配和验证已完成”。
- 用户要求“完成适配和验证”时，默认目标至少是 S3：交付可执行的数据准备、NPU 运行、结果比较和报告闭环，并同时完成主要精度/质量与性能验证。因硬件、权重、授权或外部服务阻塞时，状态必须保留为未完成/待验收并报告确切原因。
- dummy、随机输入和少量 smoke 样例最多证明 S2 功能链路，不得作为精度、质量或性能验收结论。
- 除非固定版本的官方 harness 已完全覆盖且文档给出精确命令，每个模型必须提供或复用正式的数据准备和评测/比较入口；仅在 `ACCEPTANCE_PLAN.md` 描述计算方法不等于已交付工具。
- 当公开基线无法隔离 patch 影响、适配改变算法语义或预后处理，或目标仓维护者明确要求时，patch 后 CPU/CUDA 同设备回归为强制项；其他情况属于推荐证据。多组结果必须写入不同路径，不得覆盖。
- 提交前必须从文档声明的目录、干净源码和环境执行最低正式路径的 clean-room 重放，核对命令参数、文件路径、模型服务名、context、dataset split 和输出路径；不得依赖 `.codex-reference`、历史缓存、未记录环境变量或手工步骤。
- 正式文档中的命令至少要实际执行 `--help`、dry-run 或轻量 fixture；无法运行的外部命令必须记录未验证原因，不得仅凭静态审阅声称可用。
- 使用 `python3 tools/audit_model_delivery.py <model_dir>` 执行工作区结构门禁；准备上库时增加 `--target-readiness --target-path ACL_PyTorch/built-in/<领域>/<模型目录>`。审计通过不代表运行、验收或 PR 门禁通过。
- NPU 环境不得先从 PyTorch CPU wheel 索引安装 `torch/torchaudio` 再追加 `torch-npu`；CPU 和 NPU 必须使用独立环境，NPU 侧固定 CANN 配套的完整 wheel 版本或基础镜像 digest。
- 使用 ONNX Runtime CANN EP 时，必须给出可执行的 `onnxruntime-cann` 安装版本/命令或内部 wheel 文件名与 SHA。
- L2 优先使用原始公开 benchmark 全量数据和官方配置；无法取得官方数据或 recipe 时，使用公开数据或内部固定集并明确降级口径。功能样例数和 L2 样本数必须与固定文件、官方 split 或确定性数据准备命令一致；L1/L3 和长稳扩展项不是当前准入硬门禁。
- vLLM 嵌套配置必须按固定版本 `--help` 的真实 JSON/CLI 语法书写。
- 外部数据集、评测器和 agent 仓库必须 checkout 固定 commit，不得只固定模型源码而让 evaluator 或 data HEAD 漂移。

# 文档组织

- 除非用户明确要求，不修改模型原始 `README_old.md`。
- 每个模型默认维护三类主文档：`README.md` 面向正式上库和用户；`NPU_ADAPTATION.md` 记录版本边界、代码分析、适配实现和实际验证事实；`ACCEPTANCE_PLAN.md` 记录原始指标、评测方案、对齐标准、验收结果和报告模板。
- 不再默认新增 `ANALYSIS.md`、`NPU_VALIDATION.md` 或专项评测文档；存量内容无损合并到上述职责对应的文档后再清理。同一环境、命令、指标或结论只在职责所属文档完整维护，其他文档使用摘要和引用。
- 内部文档可以引用 `README.md`，但正式上库 README 不得反向依赖默认排除的内部文档。所有路径、参数和命令必须与实际候选文件一致。

# 最小化修改与简单设计

- 仅修改 NPU 适配和验收所必需的内容，避免无关重构、格式化和重命名。
- 遵循简单设计四原则：通过测试、表达意图、消除重复、减少不必要的结构。
- 优先复用原有实现，避免跨文件复制相同逻辑，也不得为形式去重而过度抽象或扩大范围。
- 保持原有 CPU/CUDA 行为不变，将 NPU 差异限制在必要边界内。
- 优先使用原项目现有入口；只有确无合适入口且用户明确同意时才新增代码文件。明显增加审阅成本或偏离这些原则时，必须说明原因和取舍。

# 代码变更与适配脚本

- 未经明确允许，不得在代码或 patch 中使用静态检查屏蔽注释，如 `# noqa`、`# pylint:`、`# type: ignore`、`# pragma: no cover`、`# eslint-disable`、`@ts-ignore`、`@ts-expect-error`、`# pyright:`。应修复根因，或通过项目级配置统一处理。
- 一次性分析、调试或批量替换脚本原则上不得合入。流程必需的新脚本必须整理为可维护工具，并说明用途、依赖、参数、输入输出、示例和适用范围。
- 代码修改必须可复现，不得依赖未记录的手工修改、覆盖复制、运行时 monkey patch 或不可复现的批量替换。
- 目标仓受版本控制的代码优先直接提交最小修改。仅在必须保持第三方源码原样或构建/安装阶段应用修改时保留独立 patch，并提供目标版本、应用方法和验证方式。
- 新增源码、脚本和复制自上游的文件必须审查许可证与版权来源；独立 `LICENSE`、版权头、数据/权重链接按目标仓当前贡献规范和上游许可证逐项记录，不确定时不得臆测兼容。
- 适配、评估和数据准备脚本默认将必需依赖置于顶层导入；仅设备后端注册模块可按设备条件导入，例如只在 NPU 路径导入 `torch_npu`。
- 缺少依赖或官方字段、上游版本不兼容、官方评估组件不可用时，应立即暴露原始错误，不得添加不必要的兼容层或静默回退。除非作为独立非官方模式明确记录并验证，不得用基础 normalizer、名称相近的第三方包、简化指标、CPU 回退或远程下载回退替代官方评估路径。
