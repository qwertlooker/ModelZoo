# 项目约束

- 在本仓库中完成请求的修改后，默认将修改提交到当前分支。
- 提交后，默认将当前分支推送到已配置的远程仓库。
- 除非明确要求，否则不要强制推送或重写已发布历史。
- 如果推送因认证、权限、网络或远程分支分叉等原因失败，请报告确切的失败信息，并保留本地提交不变。

# 项目背景与交付目标

- 本项目用于将推理模型规范迁移并适配至昇腾 NPU。
- 待迁移模型已在 [Ascend-SACT](https://ai.gitcode.com/Ascend-SACT) 代码仓中完成初步适配和基础测试；该仓中的实现仅作为迁移起点和参考，不代表已满足正式合入标准。
- 当前工作的目标是基于已有初步适配，按照本项目规范完成代码审查、规范化修改、原始模型结果对齐和完整验证，并将验收通过的实现合入 [ModelZoo-PyTorch `ACL_PyTorch/built-in`](https://gitcode.com/Ascend/ModelZoo-PyTorch/tree/master/ACL_PyTorch/built-in) 正式代码目录。
- 迁移过程中应记录参考实现的来源和版本，并以正式代码仓的目录结构、代码规范、依赖约束、接口要求及验收标准为最终依据，不得未经审查直接照搬初步适配结果。

# 验收主线约束

- 当前工作是模型 NPU 适配迁移时，`ACCEPTANCE_PLAN.md` 和验收报告的第一优先级必须是：确认原始模型使用的测试集、原始模型公开/官方指标，以及 NPU 适配后如何对齐原始模型结果。
- 每个模型都必须先盘点并记录“原始测试集是什么、原始指标是多少”：包括数据集名称、split、样本规模、metric、normalizer/后处理、decode/推理参数、checkpoint/版本和来源链接；如果官方未发布测试集或指标，必须明确写“官方未发布”，不得编造或用自定义指标冒充官方指标。
- NPU 验收默认是迁移对齐验收：将 NPU 适配后的结果与原始模型的公开/官方指标对齐。对齐基准可来自论文、GitHub/HuggingFace 官方数据或业界公开数据，不要求必须在同环境、同条件下重新运行 CPU/CUDA 原始路径；适配的核心目标是模型在 NPU 上可运行且精度/性能不下降。当前 L2 必须同时报告主要精度/质量指标和性能指标；稳定性、内部扩展集和人工主观评测可作为补充。
- dummy、随机输入、少量 smoke 样例只能证明链路可运行，不得作为精度、质量或性能验收结论。

# 完成状态与交付硬门禁

- 必须区分“分析完成”“静态适配完成”“可运行验证完成”“迁移验收完成”和“正式验收通过”。仅有版本取证、文档、patch apply/compile 或 smoke test 时，不得使用“适配和验证已完成”。
- 用户要求“完成适配和验证”时，默认目标至少是：形成可执行的数据准备、NPU 运行、与原始公开/官方指标的结果比较和报告闭环；原始基线可引用论文、GitHub/HuggingFace 官方或业界公开数据，不强制要求在本环境重跑 CPU/CUDA。因硬件、权重、授权或外部服务无法执行时，任务状态必须保留为未完成/待验收，并报告确切阻塞。
- 除非官方固定 harness 已能直接覆盖且文档给出精确命令，每个模型必须提供或复用正式的数据准备和评测/比较入口。`ACCEPTANCE_PLAN.md` 中写“逐字段比较”“计算指标”不等同于已经交付相应工具。
- 若同时在本地运行 CPU/CUDA 与 NPU 对照，结果必须写入不同路径，不得用同一个输出文件覆盖对照组。验收至少区分：原始公开/官方基线（可来自论文、GitHub/HuggingFace 或业界公开数据）与应用 patch 后的 NPU 结果；当本地具备 CPU/CUDA 环境并选择重跑时，再额外区分应用 patch 后的 CPU/CUDA 回归结果，但不作为强制要求。
- 提交前必须做 clean-room 重放：从文档声明的目录和干净源码/环境开始，逐条执行最低正式路径；检查命令参数、文件路径、模型服务名、context、dataset split 和输出路径真实一致。不能依赖 `.codex-reference`、历史缓存、未记录的环境变量或手工步骤。
- 发现命令未经执行时，不得仅凭“看起来合理”写入正式文档。至少运行 `--help`/dry-run/轻量 fixture；无法运行的外部命令必须记录未验证原因。
- 使用 `python tools/audit_model_delivery.py <model_dir>` 作为文档结构基础门禁；该工具通过不代表运行和验收通过，仍需提供命令输出、结果文件和报告证据。
- NPU 环境不得先从 PyTorch CPU wheel 索引安装 `torch/torchaudio` 再追加 `torch-npu`；CPU 和 NPU 必须使用独立环境，NPU 侧固定 CANN 配套的完整 wheel 版本或基础镜像 digest。
- 使用 ONNX Runtime CANN EP 时必须给出可执行的 `onnxruntime-cann` 安装版本/命令或内部 wheel 文件名与 SHA，不能只写“安装配套 CANN EP”。
- 当前最低验收只要求“功能验证 + L2 正式对齐”，但 L2 必须同时完成主要精度/质量和性能验证。优先对齐原始公开 benchmark 全量数据和官方评测配置；官方数据或 recipe 不可取得时，使用公开数据或内部固定集并明确降级口径。
- 功能样例数和 L2 样本数必须与仓内固定文件、官方 split 或确定性数据准备命令一致；L1/L3 和长稳扩展项不作为当前准入硬门禁。
- vLLM 嵌套配置必须按固定版本 `--help` 的真实 JSON/CLI 语法书写。
- 所有外部数据集、评测器和 agent 仓库都必须 checkout 固定 commit；仅固定模型源码而让 evaluator/data HEAD 漂移，视为不可复现。

# 文档组织约束

- 除非用户明确要求，不要修改各模型目录中的原始 `README_old.md`。
- 每个模型默认维护三类主文档：`README.md` 面向正式上库和用户使用；`NPU_ADAPTATION.md` 记录版本边界、代码分析、适配实现及实际验证事实；`ACCEPTANCE_PLAN.md` 记录原始指标、评测方案、对齐标准、验收结果和报告模板。
- 不再默认新增 `ANALYSIS.md`、`NPU_VALIDATION.md` 或专项评测说明等分散文档；相关内容应分别合并到 `NPU_ADAPTATION.md` 和 `ACCEPTANCE_PLAN.md`。存量文档合并前不得丢失有效信息。
- 三类主文档应各司其职并通过链接交叉引用，避免重复维护同一段环境、命令、指标或验证结论；文档中的路径、参数和命令必须与实际交付文件一致。

# 最小化修改与简单设计

- 仅修改 NPU 适配和验收所必需的内容，避免无关重构、格式化和重命名。
- 遵循简单设计四原则：通过测试、表达意图、消除重复、减少不必要的结构。
- 优先复用原有实现，尤其避免跨文件复制相同逻辑；同时不得为形式去重而过度抽象或扩大修改范围。
- 保持原有 CPU/CUDA 行为不变，并将 NPU 差异限制在必要边界内。
- 优先在原项目现有入口上形成最小、清晰的 Git patch/diff；只有在原项目确实没有合适入口且用户明确同意时，才新增代码文件。
- 若实现明显增加审阅成本或偏离上述原则，必须说明原因和取舍。

# 代码变更与辅助脚本约束

- 未经明确允许，不得在代码中使用静态检查屏蔽注释（如 `# noqa`、`# pylint:`、`# type: ignore`、`# pragma: no cover`、`# eslint-disable`、`@ts-ignore`、`@ts-expect-error`、`# pyright:` 等）。遇到静态检查告警应修复根因或通过项目级配置（如 `setup.cfg`/`.flake8`/`pyproject.toml` 的 `per-file-ignores`）统一处理，不得在代码内逐行屏蔽。新增或修改的 patch 文件同样适用此约束。
- 一次性分析、调试或批量替换脚本原则上不得合入正式代码。若适配、数据准备或验收流程必须依赖新增脚本，应将其整理为可维护的正式工具，并提供用途、依赖、参数、输入输出、运行示例及适用范围说明。
- 代码修改必须可复现；不得依赖未记录的手工修改、覆盖复制、运行时 monkey patch 或不可复现的批量替换操作。
- 对目标仓内受版本控制的代码，应优先直接提交最小化修改；仅在必须保持第三方源码原样或需要在构建、安装阶段应用修改时，才保留独立 patch 文件，并同时提供 patch 的目标版本、应用方法和验证方式。

# 项目范围的适配脚本标准

- 这些标准适用于 ModelZoo 中的每个模型目录，不仅限于 Canary-1B。
- 对于适配、评估和数据准备脚本，默认将必需依赖保留为顶层导入。设备后端注册模块可以按条件导入，例如仅在 NPU 路径中导入 `torch_npu`。
- 不要添加不必要的兼容层或静默回退。缺少依赖、缺少官方字段、上游版本不兼容或官方评估组件不可用时，应尽快失败并暴露原始错误。
- 除非明确记录并验证为独立的非官方模式，否则不要用正则/基础规范化器、名称相近的第三方包、简化指标、CPU 回退或远程下载回退替代官方评估路径。
- 遵循 `模型NPU 适配标准流程.md`，将其作为项目级工作流程的权威来源。
